# .env.example
# Alleen VITE_* variabelen; geen secrets in code.
VITE_APP_NAME=FitFi
VITE_ENV=development

# SSE/analytics in dev standaard UIT (degradeer gracieus)
VITE_SSE_ENABLED=false
VITE_ANALYTICS_ENABLED=false

# Afbeelding optimalisatie/proxy flags (optioneel)
VITE_IMAGE_OPTIMIZATION=true