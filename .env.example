# Supabase configuratie (VERPLICHT voor productie)
VITE_SUPABASE_URL=your_supabase_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# Nova AI Configuration (Netlify Functions only - NOT exposed to client)
# Set these in Netlify dashboard: Site settings → Environment variables
NOVA_UPSTREAM=on
OPENAI_API_KEY=sk-your-openai-api-key

# AWIN Affiliate Configuration (optional, privacy-first)
# VITE_AWIN_ENABLED: Set to 'true' to enable AWIN MasterTag (default: false)
# VITE_AWIN_MERCHANT_ID: Your AWIN Advertiser ID (from AWIN dashboard → Settings → Account Details)
# Note: MasterTag only loads on /results, /outfits, /dashboard AND requires user consent (marketing cookies)
VITE_AWIN_ENABLED=false
VITE_AWIN_MERCHANT_ID=2061345

# Optioneel: affiliate partner id (geen secret)
VITE_AFFILIATE_PARTNER_ID=

# Contact Email (optioneel - wordt getoond op contact pagina)
VITE_CONTACT_EMAIL=hello@fitfi.ai

# Google Analytics 4 (optioneel - voor tracking en analytics)
# VITE_GTAG_ID: Your GA4 Measurement ID (format: G-XXXXXXXXXX)
# Find it in: Google Analytics → Admin → Data Streams → Select your stream → Measurement ID
# Note: Analytics only loads when users accept analytics cookies via cookie banner
VITE_GTAG_ID=

# PWA Push Notifications (VERPLICHT voor push notifications)
# Generate VAPID keys with: npx web-push generate-vapid-keys
# Public key goes here (exposed to client), private key stays on server (Netlify env vars)
VITE_VAPID_PUBLIC_KEY=
# VAPID_PRIVATE_KEY= (Add to Netlify/Supabase env vars - NEVER commit to repo!)

# Canonical Host (gebruikt voor URL-rewriting in build)
VITE_CANONICAL_HOST=https://fitfi.ai

# Supabase gebruiksmodus (true = gebruik Supabase, false = gebruik lokale data)
VITE_USE_SUPABASE=true

# Supabase health check instellingen
VITE_SUPABASE_HEALTHCHECK_TABLE=products
VITE_SUPABASE_HEALTHCHECK_TIMEOUT_MS=3500
VITE_SUPABASE_RETRY_MAX_ATTEMPTS=3
VITE_SUPABASE_RETRY_BASE_MS=400

# Nova / FitFi tier (visitor | member | plus | founder)
VITE_FITFI_TIER=visitor

# Shop partner voor deeplinks (zalando | amazon | bol)
VITE_DEFAULT_SHOP_PARTNER=zalando

# Amazon affiliatetag (optioneel, voor Amazon deeplinks)
VITE_AMAZON_TAG=

# Netlify Image CDN (true = gebruik Netlify CDN voor afbeeldingen)
VITE_USE_NETLIFY_IMAGE_CDN=true

# Geavanceerde analytics inschakelen (true/false)
VITE_ADVANCED_ANALYTICS=true

# Debug logging in productie inschakelen (standaard false)
VITE_ENABLE_LOGS=false

# Build-identifier (optioneel, voor Sentry / monitoring)
VITE_BUILD_TAG=