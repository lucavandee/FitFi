#!/bin/sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🧪 Running tests before push..."
if npx -y vitest --version >/dev/null 2>&1; then
  npx vitest --passWithNoTests --run || exit 1
else
  echo "ℹ️ Vitest niet gevonden; tests overgeslagen"
fi
